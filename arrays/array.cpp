#include<iostream>
using namespace std;
int main()
{
    int n,a[n];
    cout<<"enter the size of array : ";
    cin>>n;
    cout<<endl<<"enter the elements of array "<<endl;
    for(int i=0;i<=n;i++)
    {
        cin>>a[i];
    }
    cout<<1;
   // getmax(a,n);
   // getmin(a,n);
}

